#!/usr/bin/env ruby

$LOAD_PATH.unshift File.dirname(__FILE__) + '/../lib'
require 'bane'

failure_policy = lambda { |message| puts message; exit 1; }
config = Bane::Configuration.from(ARGV, failure_policy)

launcher = Bane::Launcher.new(config.servers)
launcher.start
launcher.join
